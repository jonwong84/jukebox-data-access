# Jukebox.DataAccess

Data access layer for the Jukebox API providing CRUD operations for song metadata.

Within the architecture of the Jukebox metadata API, this handles the processing of data from external datastores, and is meant to be referenced by a manager layer.